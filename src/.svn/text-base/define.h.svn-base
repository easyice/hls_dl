#ifndef DEFINE_H
#define DEFINE_H

#include <unistd.h>
#include <sys/syscall.h>
#include "MuxLog.h"

typedef struct _IO_EVENT_T
{
	int fd;
	
}IO_EVENT_T;




#define MAX_USER_FD 32000

#define USER_SEND_BUFFER_SIZE (4096*32)


#define gettid() syscall(__NR_gettid)



// #define DEBUG
// 
// #ifdef DEBUG
// #define debug_log(args...) printf(args)
// #else
// #define debug_log(args...) 
// #endif



typedef enum _SEND_RES_T
{
        RES_ACTIVE_CLOSE = -3,
	RES_ERROR = -2,
	RES_AGAIN = -1,
	RES_SUCCESS = 0
}SEND_RES_T;



typedef enum _REQ_TYPE_T
{
	REQ_TYPE_M3U8,
	REQ_TYPE_TS,
        REQ_TYPE_CTL,
	//REQ_TYPE_NORNAL_TS,
	//REQ_TYPE_TRICK_TS,
	REQ_TYPE_TEST_PAGE,
	REQ_TYPE_TEST_VIDEO,
	REQ_TYPE_UNKNOWN //不可使用
}REQ_TYPE_T;











#endif

